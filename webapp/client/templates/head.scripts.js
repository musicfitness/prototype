// include global vendor scripts to head element
Meteor.startup(function () {
  /*
  var bowerFolder = '/bower_components';
  var vendorScripts = [
    bowerFolder + '/bootstrap-sass/assets/javascripts/bootstrap.min.js'
    ];
  $.each(vendorScripts, function (index, value) {
    var scriptTag = '<script type="text/javascript" src="' + value + '"></script>';
    $('head').append(scriptTag);
  });
  */
});